# Neon Database Configuration Example
# Copy this to .env and update with your actual Neon credentials

DB_HOST=your-project-name.region.aws.neon.tech
DB_PORT=5432
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=your_database

# SSL Configuration for Neon (required)
DB_SSL_MODE=require
DB_CHANNEL_BINDING=require

# Server Port
PORT=3000

# Full connection string format (for reference):
# postgresql://user:password@host/database?sslmode=require&channel_binding=require
